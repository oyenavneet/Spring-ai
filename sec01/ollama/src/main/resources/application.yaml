spring:
  application:
    name: ollama
  ai:
    model:
      chat: ollama
    ollama:
      chat:
        options:
          model: llama3.2:1b


logging:
  pattern:
    console: "%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n"